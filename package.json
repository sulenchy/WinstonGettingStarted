{
  "name": "winstongettingstarted",
  "version": "1.0.0",
  "description": "Getting  started with winston express module",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && babel-node server/app.js",
    "build": "./node_modules/.bin/babel server -d ./lib",
    "lint": "./node_modules/.bin/eslint server/** "
  },
  "author": "sulenchy",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^5.14.1"
  }
}
